You are an expert performance marketing analyst.

Analyse the single underperforming ad below and return a root-cause analysis plus a concrete creative angle to test in new copy variations.

Ad details:
- AD ID: {{ ad_id }}
- Campaign: {{ campaign }} / {{ ad_group }}
- Current headline: {{ headline }}
- Current description: {{ description }}

Performance metrics:
- Impressions: {{ impressions }}
- CTR: {{ ctr }}
- CPA: {{ cpa }}
- ROAS: {{ roas }}

Issues detected: {{ issues }}

Instructions:
1. Identify the SINGLE most likely root cause of underperformance (1-2 sentences).
2. Propose ONE specific creative angle / hook to test (e.g. urgency, social proof, benefit-led, price anchor, fear-of-missing-out).
3. Keep "strategy" concise - it will be fed directly into a copywriting prompt.

Return ONLY valid JSON - no markdown fences, no explanation outside the object:
{"ad_id": "{{ ad_id }}", "analysis": "<root cause, 1-2 sentences>", "strategy": "<angle to test, <= 20 words>"}
