name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install linters
        run: python -m pip install black==24.10.0 ruff==0.9.6
      - name: Format check
        run: black --check .
      - name: Lint
        run: ruff check .

  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[all,dev]"
      - name: Tests
        run: pytest -q

  smoke:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install (core only — no UI or connector extras)
        run: python -m pip install .
      - name: End-to-end dry run with rendering
        run: |
          gcf --version
          gcf run --input examples/ads_sample.csv --out smoke/pipeline --mode dry --max-creatives 12
          gcf create --product "Cloudstep running shoes" --offer "30% off launch week" \
            --benefit "Featherlight 180g build" --n 6 --out smoke/studio --formats all
          gcf render --input examples/posts_sample.csv --out smoke/sheet --formats square,story
          test -f smoke/pipeline/gallery.html
          test "$(find smoke -name '*.png' | wc -l)" -ge 40
      - name: Rebuild the README proof set (gcf demo)
        run: |
          gcf demo --out smoke/demo
          for f in 01-before-after 02-one-run-at-scale 03-brief-to-posts-en \
                   04-brief-to-posts-vi 05-one-copy-any-brand 06-every-placement; do
            test -s "smoke/demo/$f.jpg"
          done
          python -c "import json; d=json.load(open('smoke/demo/demo.json')); assert d['images']['02-one-run-at-scale.jpg']['images'] >= 50, d"
      - name: Upload rendered creatives and demo set
        uses: actions/upload-artifact@v4
        with:
          name: rendered-creatives
          path: smoke/
          retention-days: 14
